"""
download_wikidata.py
compute_relation_stats_60k.py
sample_wikidata_triples.py
merge_chunks.py

convert_for_mTransE_csv.py

follow training in repo ...
(mention how I turned off the alignment during the training)

add visualizations (in another separate repo for me)

compute_relation_stats_42k.py
sample_42k_from_60k.py

truncate_descriptions.py (to run in Colab)


append_cosine.py (on GPU) ! push to GPU first

create_2_formats.py for desc and labels only , also converting to csv
    
dataset_split.py in colab

then save in another project under separate dirs for each lang


"""